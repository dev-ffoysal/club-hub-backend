export enum PaymentType {
  CLUB = 'club',
  EVENT = 'event',
}

export enum PaymentStatus {
  PENDING = 'pending',
  PAID = 'paid',
  FAILED = 'failed',
}

export enum PaymentMode {
  CASH = 'cash',
  ONLINE = 'online',
}
